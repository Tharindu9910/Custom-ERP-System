{
  "name": "erp",
  "private": true,
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "pnpm": ">=9.12.0 <10.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "db:migrate": "pnpm --filter api migration:run",
    "db:seed": "pnpm --filter api seed"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}